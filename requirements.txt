# pip fallback (prefer environment.yml + micromamba).
# Install PyTorch first with the right backend, e.g. for CUDA 12.8:
#   pip install torch==2.9.1 --index-url https://download.pytorch.org/whl/cu128
# or CPU-only:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.4
transformers>=4.50,<5
accelerate
numpy<2.3
scipy
matplotlib
seaborn
tqdm
pytest
datasets   # only needed for scripts/perplexity_eval.py (WikiText-2)
